---
- name: Listen for Kafka events
  hosts: localhost
  sources:
    - name: Listen to Kafka server broker
      ansible.eda.kafka:
        topic: eda-topic
        host: eda-demo-kafka-bootstrap
        port: 9092
  rules:
    - name: Debug demo event
      condition: event.message != ""
      action:
        run_job_template:
          name: "Demo Job Template"
