---
- name: Launch the patching workflow
  hosts: localhost

  tasks:
    - name: Show me all the things
      ansible.builtin.debug:
        var: ansible_facts
    - name: my_fact? 
      ansible.builtin.debug:
        var: football